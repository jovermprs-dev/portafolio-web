---
import type { Content, Lang } from "../i18n/content";

interface Props {
  lang: Lang;
  about: Content["about"];
}

const { lang, about } = Astro.props;
---

<section id={`about-${lang}`} class="about wrapper">
  <h2>{about.heading}</h2>
  <p>{about.paragraph}</p>
</section>
